# Install a CUDA-matched PyTorch build first if the RunPod image does not include one.
# The remaining packages are the minimal stack for one contrastive SDF LoRA run.
transformers>=4.55.0
accelerate>=1.10.0
peft>=0.17.0
datasets>=3.6.0
huggingface_hub>=0.34.0
safetensors>=0.5.3
wandb>=0.19.0
