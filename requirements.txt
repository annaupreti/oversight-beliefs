# The Docker image is the reproducibility boundary. Torch must stay on CUDA
# 12.8: bitsandbytes 0.46.1 ships a CUDA 12.8 binary, but not CUDA 13.0.
# Install these three wheels from the PyTorch cu128 index on a bare RunPod pod.
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
transformers==4.56.2
trl==0.22.2
accelerate==1.10.1
peft==0.17.1
datasets==4.4.2
bitsandbytes==0.46.1
huggingface_hub[hf_xet]==0.35.1
safetensors==0.6.2
wandb==0.21.4
unsloth
